spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/JPAPG
    username: postgres
    password: mysecretpassword
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
      use_sql_comments: true
